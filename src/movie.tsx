import {NativeStackScreenProps} from '@react-navigation/native-stack';
import {RootStackList} from '../App';
import {View} from 'react-native';

export const Movie = ({navigation}: NativeStackScreenProps<RootStackList>) => {
  return <View></View>;
};
